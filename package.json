{
  "name": "@micro-js/handle-actions",
  "description": "Compose a reducer from a map of action handlers",
  "repository": "git://github.com/micro-js/handle-actions.git",
  "version": "1.0.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/compose-reducers": "^1.0.0",
    "@micro-js/is-undefined": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
